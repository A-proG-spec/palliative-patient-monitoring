// src/lib/axios.ts
// Re-export the configured axios instance from the API client layer.
// Components/hooks that need the raw instance can import from here.

export { default } from '@/api/client';
